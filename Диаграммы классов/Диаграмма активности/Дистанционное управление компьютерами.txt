@startuml
start
:заходим в дистанционное управление компьютерами;
:переходим в список компьютеров;
repeat
  if(время 22:05) then (да)
    :выключить все компьютеры;
  else (нет)  
  endif
  if(время 6:00) then (да)
    :включить все компьютеры;
  else (нет)  
  endif
  if(забронировать компьютер) then (да)
  :выбрать компьютер;
    if (компьютер свободен)then(нет)
      :выбрать другой компьютер;
    else (да)
    :бронь компьютера;
    endif
  else (нет)  
  endif
repeat while (компьютер выбран?) is (нет) not (да)

stop
@enduml