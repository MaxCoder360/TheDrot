@startuml

start

if (принятие оплаты) then (картой)

    :произвести оплату картой;
    if (проблемы с оплатой) then (да)
        if (принятие оплаты(2)) then (картой)
            :произвести оплату картой;
            if (проблемы с оплатой) then (да)
                :произвести оплату наличными;
            else (нет)
                :произвести оплату картой;
            endif

        else (наличными)
            :принятие оплаты наличными;

        endif


    else (нет)
    endif

else (наличными)

  :произвести оплату наличными;

endif

stop

@enduml