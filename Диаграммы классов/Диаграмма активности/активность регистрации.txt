@startuml
start
:вход в систему;
repeat
:ввод персональных данных нового пользователя;
:ввод электронной почты;
  if (введённая электронная почта неверна) is (да) then
    :ввести корректную электронную почту;
  else (нет)
  endif
:ввод пароля;
if (введённый пароль неверен) is (да) then
    :ввести корректный пароль;
  else (нет)
  endif
repeat while (введённые данные корректны?) is (нет)  not (да)
:нажать на кнопку "регистрация";

stop
@enduml